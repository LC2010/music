var Note = function(config){
    var self    = this;
    self.wrapId = id;
    self.inited = false;
    
}
Note.prototype = {
    init : function(){
        var self = this;
        var opt  = config;
        var boldBtn = $('#'+config.bold);
        var underlineBtn = $('#'+config.underline);
        var image = $('#'+config.image);
    }
    

}
